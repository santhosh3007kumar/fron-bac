version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/MEANAPP

hooks:
  AfterInstall:
    - location: scripts/install_dependencies.sh
      timeout: 300
      runas: ubuntu

  ApplicationStart:
    - location: scripts/start_services.sh
      timeout: 300
      runas: ubuntu